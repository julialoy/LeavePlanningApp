{
  "name": "leave-planning-app",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "shared",
    "web",
    "mobile"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "mobile": "npm run start --workspace=mobile"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
